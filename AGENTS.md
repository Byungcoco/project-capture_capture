# Capture Capture — AI 작업 규칙

이 파일은 이 저장소에서 작업하는 Codex, Claude, Gemini, Copilot과 기타 LLM 도구의 공통 원본이다. 다른 지침 파일은 이 파일을 가리키는 포인터만 유지한다.

## 우선순위

기한 내 플레이 가능한 웹 빌드 제출이 다른 모든 목표보다 우선한다. 절차가 제출을 위협하면 절차를 줄이고, 줄인 사실을 ADR로 남긴다 (ADR-0006).

## 최소 규칙

1. 저장소 전체를 읽지 말고 `docs/wiki/index.md`부터 읽는다.
2. 파일 탐색은 `rg --files`, 내용 검색은 `rg`를 우선하고 현재 작업에 필요한 원본만 연다.
3. 구현 전 `docs/wiki/05-progress/current.md`에서 현재 목표와 범위를 확인한다.
4. 확정된 사실의 원본은 하나만 둔다. 다른 문서에는 요약을 복제하지 말고 링크한다.
5. 새로운 기술·제품 판단은 `docs/wiki/04-decisions/`에 ADR로 남긴다.
6. 작업 또는 대화 중 재사용 가치가 생긴 정보는 완료 전에 위키 원본 문서에 반영한다.
7. 검증 전 정보는 `status: draft`, 직접 실행하거나 공식 자료로 확인한 정보만 `verified` 또는 `accepted`로 표시한다.
8. `_meta` 인덱스와 `90-logs`의 생성 로그를 직접 편집하지 않는다. `python tools/wiki_sync.py ...`를 실행한다.
9. 완료 전 `python tools/wiki_sync.py finalize --summary "작업 요약" --category <유형> --actor <도구>`를 실행한다.
10. 결론부터 보고하고, 실행하지 않은 검증을 실행했다고 표현하지 않는다.
11. 동작이 바뀌는 코드는 실패하는 테스트를 먼저 작성하고 실패를 눈으로 확인한 뒤 구현한다. 상세는 `TDD 기본 규칙`을 따른다.
12. 계획을 서브에이전트에게 실행시킬 때는 `.agents/skills/dispatch/SKILL.md`를 읽고 따른다.
13. 신뢰 경계를 지나는 변경은 보안 검토를 수행하고 결과를 완료 보고에 남긴다. 상세는 `보안 검토 기준`을 따른다.

## LLM 시작 순서

1. `docs/wiki/index.md`를 읽는다.
2. `docs/wiki/05-progress/current.md`를 읽는다.
3. `docs/wiki/_meta/wiki-index.md` 또는 `search-catalog.json`에서 관련 문서를 찾는다.
4. 현재 작업과 직접 관련된 원본만 추가로 연다.
5. 코드 작업이면 `docs/wiki/06-code/index.md`와 관련 구현 분야 하나를 읽는다.
6. 과거 변경 이유가 필요할 때만 ADR과 월별 로그를 읽는다.

`docs/wiki/_meta/wiki-index.md`는 유형별 문서 목록, `tag-index.md`는 태그별 목록, `search-catalog.json`은 기계 검색용 카탈로그다. 셋 모두 생성물이므로 직접 고치지 않는다.

## Git Flow와 브랜치 작업 규칙

- `main`은 release와 tag, `develop`은 다음 release 통합 브랜치다. 둘에 직접 commit하거나 push하지 않는다.
- 일반 작업은 `develop`에서 `feature/<scope>-<slug>`를 만들고 PR로 `develop`에 병합한다.
- release는 `develop`에서 `release/<semver>`를 만들고 안정화·버전·release note만 반영한다. `main` 병합과 `vX.Y.Z` tag 뒤 반드시 `develop`에 back-merge한다.
- hotfix는 `main`에서 `hotfix/<semver-or-slug>`를 만들고 `main` 병합·tag 뒤 반드시 `develop`에 back-merge한다.
- LLM은 쓰기 작업 전에 `git status --short --branch`와 현재 브랜치를 확인한다. `main`/`develop`이면 올바른 시작점에서 목적 브랜치를 먼저 만든다.
- 사용자의 기존 수정과 untracked 파일을 보존하고 이번 작업 경로만 stage한다. shared branch rebase, force-push와 위험한 reset/checkout을 하지 않는다.
- 사용자가 명시하지 않은 fetch, pull, push, PR, tag와 release deploy는 수행하지 않는다.
- commit은 `feat:`, `fix:`, `docs:`, `build:`, `ci:`, `perf:`, `test:`, `chore:` 같은 접두사를 반드시 사용하며, 커밋 내용은 특수문자를 포함하지 않고 오직 한글로만 작성한다. 한 branch에는 하나의 검토 가능한 의도만 둔다.
- PR 전 관련 test/build와 위키 check·finalize를 실행한다.
- 커밋 히스토리와 에이전트 대화 로그는 Codex Collaboration 심사 증빙이므로 보존한다.

## 가치 있는 정보의 판정

다음 중 하나에 해당하면 대화에만 남기지 않고 위키에 기록한다.

- 구현 방식, 데이터 계약, 성능 예산처럼 이후 작업의 선택을 제한하는 결정
- 조사 결과와 공식 출처, 비교표, 채택·보류 이유
- 반복해서 사용할 명령, 복구법, 디버깅 절차, 알려진 함정
- 플레이테스트 결과, 성공 기준, 밸런스 가설과 반증
- 사용자가 확정한 제품 방향, 범위, 명칭, 플랫폼, 비목표
- 코드만 봐서는 알기 어려운 의도나 트레이드오프

일시적인 생각, 단순 진행 중계, 코드에서 즉시 알 수 있는 내용은 기록하지 않는다. 위치가 불명확하지만 가치가 있는 정보는 `docs/wiki/80-inbox/`에 임시로 남기고 같은 작업 또는 다음 정리 작업에서 원본 문서로 이동한다.

## 작업 후 문서 갱신 절차

1. 변경으로 인해 달라진 현재 사실을 해당 원본 문서에 반영한다.
2. 새로운 판단이면 ADR을 추가하고 관련 원본에서 링크한다.
3. 현재 목표나 다음 행동이 변했으면 `05-progress/current.md`만 갱신한다.
4. 코드 변경이면 공개 계약·불변식·runtime·검증에 미친 영향을 판단하고 중요한 경우 **`06-code` 원본을 반드시 직접 갱신한다. (스크립트 실행만으로 코드 위키가 업데이트되지 않음을 명심하라.)**
5. 위 1~4번의 **마크다운 원본 수정 작업이 완전히 끝난 후**, 다음 명령으로 날짜, 인덱스, 검색 카탈로그, 월별 로그를 동기화한다.

```bash
python tools/wiki_sync.py finalize \
  --summary "무엇을 바꿨는지" \
  --why "왜 바꿨는지" \
  --impact "영향받는 기능·계약·데이터" \
  --validation "실행한 검증과 결과 또는 미실행" \
  --documentation "갱신한 위키 원본 또는 불필요한 이유" \
  --category code --actor codex
```

`category` 권장값은 `code`, `docs`, `research`, `decision`, `design`, `test`, `ops`다. Git pre-commit 훅도 같은 동기화를 수행하므로 수동 실행을 놓쳐도 커밋 직전에 생성물이 갱신된다.

## 로그 정책

- 전체 작업 감사: `docs/wiki/90-logs/all/YYYY/YYYY-MM.md`
- 위키 원본 변경: `docs/wiki/90-logs/wiki/YYYY/YYYY-MM.md`
- 중요 기획 변경: `docs/wiki/90-logs/design/YYYY/YYYY-MM.md`
- 중요 코드 변경: `docs/wiki/90-logs/code/YYYY/YYYY-MM.md`
- 로그 한 사건은 두 줄만 사용한다. 첫 줄은 `what`, 둘째 줄은 `why`, `impact`, `validation`, `docs`, 변경 경로를 기록한다.
- 자세한 근거는 제품·기획·코드 위키 원본이나 ADR에 남기고 로그에는 결론을 압축한다.
- Git이 정확한 본문 차이의 원본이다. 로그는 언제, 왜, 어느 파일군이 바뀌었는지만 기록한다.

## 작업 원칙

- 게임 제목은 **Capture Capture**(가제)다. 타 게임의 고유 명칭·에셋·UI를 공개 빌드에 사용하지 않는다.
- 목표 플랫폼은 별도 설치 없이 실행되는 데스크톱 웹 브라우저다.
- 게임 루프와 시뮬레이션을 렌더·DOM 생명주기에 종속시키지 않는다. 모듈 경계와 결정론 규칙의 원본은 `docs/wiki/03-tech/architecture.md`다.
- 캡처 파이프라인의 원본은 `docs/wiki/03-tech/capture-pipeline.md`, 게임 규칙의 원본은 `docs/wiki/02-design/gameplay/game-rules.md`다. 여기에 복제하지 않는다.
- 새 의존성은 실제 필요가 생겼을 때만 추가하고, 추가 이유를 관련 ADR 또는 변경 설명에 기록한다.
- 한 번의 변경은 하나의 검증 가능한 결과에 집중한다.
- 사용자 요청과 무관한 파일을 정리하거나 대규모 포맷팅하지 않는다.
- 막히면 30분 이상 같은 접근을 반복하지 말고, 시도한 것과 막힌 지점을 정리해 보고한다.

## TDD 기본 규칙

이 규칙은 **스킬·플러그인 사용 여부와 무관하게 항상 적용된다.** 어떤 도구로 작업하든, 조정자든 서브에이전트든 같다. 스킬을 호출하지 않았다는 것은 예외 사유가 아니다.

```
실패를 관측한 테스트 없이 동작 코드를 작성하지 않는다.
```

### 순서

1. **RED** — 원하는 동작 하나를 단언하는 테스트를 먼저 쓴다.
2. **실패 확인** — 실행해서 실패시킨다. **출력을 보존한다.** 기능이 없어서 나는 실패여야 한다. 오타나 import 에러면 고치고 다시 실패시킨다.
3. **GREEN** — 통과시킬 최소 구현만 쓴다. 계획에 없는 기능을 얹지 않는다.
4. **통과 확인** — 해당 테스트와 나머지 테스트가 모두 통과하는 출력을 남긴다.
5. **REFACTOR** — 초록인 상태를 유지하며 정리한다. 동작을 추가하지 않는다.

버그 수정도 같다. 먼저 버그를 재현하는 실패 테스트를 쓴다. 테스트 없이 버그를 고치지 않는다.

### 적용 예외

산출물이 실행 코드가 아닌 작업(문서, 위키, 에셋 변환, 순수 설정값, 생성물)은 RED 대신 **실행 가능한 확인 명령**을 남긴다. 예: `python tools/wiki_sync.py check`.

렌더 연출·카메라 감각·조작 손맛처럼 자동 단언이 불가능한 튜닝 작업은 RED 대신 **브라우저에서의 확인 절차**(어떤 조작으로 무엇을 보면 되는지)를 완료 보고에 남긴다. 단 그 아래의 결정론 로직(틱·충돌·캡처 기하)은 예외가 아니다.

그 밖의 예외는 사용자 승인이 필요하다. 판단으로 건너뛰지 않는다.

### 보고

완료 보고에 **실패 출력과 통과 출력을 함께** 남긴다. "테스트를 작성하고 실행했습니다" 같은 서술은 증거가 아니다. 최소 규칙 10의 연장이다 — 관측하지 않은 통과를 통과라고 쓰지 않는다.

### 금지

- 구현을 먼저 쓰고 테스트를 나중에 붙이기
- 처음부터 통과하는 테스트를 근거로 완료 보고
- 실패 출력을 확인하지 않은 채 초록이라고 주장
- 실패한 구현을 "참고용"으로 남겨두고 그것을 보며 테스트 작성 — 지우고 다시 시작한다

## 보안 검토 기준

이 규칙은 **스킬·플러그인 사용 여부와 무관하게 항상 적용된다.**

### 적용 대상

변경이 다음 중 하나에 닿으면 보안 검토 대상이다.

- 외부 입력의 파싱과 역직렬화 (리플레이 데이터, 스테이지 데이터, URL 파라미터)
- 외부 서비스 연동 (Hive SDK, 저장·랭킹 API)
- secret, 환경 설정, CI 구성
- 로그·telemetry로 나가는 값
- 새 의존성 추가와 버전 상향

닿지 않으면 완료 보고에 `보안 검토: 해당 없음 — <이유>`를 적는다. 슬롯을 비우지 않는다.

### 검토 항목

- 외부에서 온 데이터(리플레이·공유 링크)를 검증 없이 실행 경로에 넣지 않는다. 크기·범위 상한을 둔다.
- secret은 저장소 밖에 둔다. 위키·로그·커밋·PR 본문에 값을 남기지 않는다.
- 방어 로직은 주장이 아니라 부정 케이스 테스트로 남긴다 (거부되어야 하는 입력이 실제로 거부되는지).

### 보고

완료 보고에 `보안 검토:` 한 줄을 남긴다. 대상 항목, 확인 결과, 추가한 테스트, 미해결 위험을 적는다. 미실행 검토를 실행했다고 쓰지 않는다.

## 스킬

런타임 중립 스킬은 `.agents/skills/<이름>/SKILL.md`에 둔다. Claude Code용 포인터는 `.claude/skills/<이름>/SKILL.md`에 두고 본문을 복제하지 않는다. 네이티브 스킬 탐색이 없는 도구는 아래 표의 경로를 직접 읽는다.

| 스킬 | 경로 | 쓰는 때 |
|---|---|---|
| dispatch | `.agents/skills/dispatch/SKILL.md` | 계획이 있고 서브에이전트에게 태스크 단위로 실행시킬 때 |

`.dispatch/`는 디스패치 작업 공간(원장·브리프·보고서)이며 git에 올리지 않는다.

## 검증 기준

- 동작 변경에는 가능한 범위에서 Vitest 단위 테스트를 추가한다.
- 완료 보고 전 타입 검사, 테스트, 프로덕션 빌드를 실행한다.
- 위키 변경 후 `python tools/wiki_sync.py check`로 메타데이터와 생성 인덱스를 검증한다.

## 문서 형식

- 원본 노트는 `title`, `type`, `status`, `tags`, `updated`, `summary` frontmatter를 갖는다.
- `type`은 `product`, `design`, `technology`, `research`, `decision`, `progress`, `guide`, `implementation`, `reference` 중 하나를 사용한다.
- 태그는 소문자 kebab-case로 2~6개만 사용한다. 폴더명과 제목을 그대로 중복한 태그는 피한다.
- 위키 링크는 상대 경로 Markdown 링크 또는 Obsidian wikilink를 사용한다.
- `current.md`는 현재 상태와 다음 목표만 짧게 유지한다. 완료 이력은 로그가 담당한다.
- `generated: true`가 있는 파일은 직접 편집하지 않는다.
