# Capture Capture — AI 작업 규칙

이 파일은 이 저장소에서 작업하는 Codex, Claude와 기타 LLM 도구의 공통 원본이다.
다른 지침 파일(CLAUDE.md 등)은 이 파일을 가리키는 포인터만 유지한다.

## 우선순위

기한 내 플레이 가능한 웹 빌드 제출이 다른 모든 목표보다 우선한다.
체계는 제출을 돕기 위해 존재한다. 절차가 제출을 위협하면 절차를 줄이고, 줄인 사실을 ADR로 남긴다.

## 최소 규칙

1. 저장소 전체를 읽지 말고 `docs/index.md`부터 읽는다.
2. 구현 전 `docs/05-progress/current.md`에서 현재 목표와 범위를 확인한다.
3. 확정된 사실의 원본은 하나만 둔다. 다른 문서에는 복제하지 말고 링크한다.
4. 새로운 기술·제품 판단은 `docs/04-decisions/`에 ADR로 남긴다. 왜 그 선택을 했는지가 반드시 담겨야 한다.
5. 사람이 직접 수정한 코드와 untracked 파일을 보존한다. 의도를 모르는 변경은 삭제하지 말고 질문한다.
6. 지시받지 않은 대규모 리팩터링, 파일 구조 변경, 의존성 추가는 먼저 제안만 한다.
7. 한 번의 변경은 하나의 검증 가능한 결과에 집중한다.
8. 결론부터 보고하고, 실행하지 않은 검증을 실행했다고 표현하지 않는다.
9. 막히면 30분 이상 같은 접근을 반복하지 말고, 시도한 것과 막힌 지점을 정리해 보고한다.

## LLM 시작 순서

1. `docs/index.md`를 읽는다.
2. `docs/05-progress/current.md`를 읽는다.
3. 현재 작업과 직접 관련된 원본만 추가로 연다.
   (설계 → `02-design/`, 아키텍처·파이프라인 → `03-tech/`, 과거 결정 이유 → `04-decisions/`)

## Git 규칙

- `main`은 안정 브랜치, `develop`은 통합 브랜치다. 둘에 직접 commit하지 않는다.
- 작업은 `develop`에서 `feature/<scope>-<slug>`를 만들고 PR 또는 병합으로 `develop`에 합친다.
- 제출용 빌드 시점에 `develop`을 `main`에 병합하고 태그를 남긴다.
- LLM은 쓰기 작업 전 `git status --short --branch`로 현재 브랜치를 확인한다. `main`/`develop`이면 목적 브랜치를 먼저 만든다.
- commit은 `feat:`, `fix:`, `docs:`, `build:`, `test:`, `chore:` 접두사를 사용하고 내용은 한글로 작성한다.
- 사용자가 명시하지 않은 push, PR, tag는 수행하지 않는다.
- 커밋 히스토리와 에이전트 대화 로그는 Codex Collaboration 심사 증빙이므로 보존한다.

## 테스트 규칙 (범위 적용 TDD)

**결정론적 순수 로직 모듈은 실패를 관측한 테스트 없이 동작 코드를 작성하지 않는다.**

적용 대상: `core/`의 시뮬레이션·충돌·틱, `capture/`의 투영·볼록 껍질·클리핑, `stamp/`의 배치 판정 등
입력→출력이 결정적인 모든 모듈. RED(실패 출력 확인) → GREEN(최소 구현) → REFACTOR 순서를 지키고,
완료 보고에 실패 출력과 통과 출력을 함께 남긴다. 버그 수정도 재현 실패 테스트를 먼저 쓴다.

적용 예외: 렌더 연출, 카메라 감각, 점프 손맛 같은 튜닝 코드는 RED 대신 **실행 가능한 확인 방법**
(브라우저에서 어떤 조작으로 무엇을 보면 되는지)을 완료 보고에 남긴다.
그 밖의 예외는 사용자 승인이 필요하다. 판단으로 건너뛰지 않는다.

## 작업 후 문서 갱신 절차

1. 변경으로 달라진 현재 사실을 해당 docs 원본에 반영한다.
2. 새로운 판단이면 ADR을 추가한다 (`docs/_templates/adr.md` 양식).
3. 현재 목표나 다음 행동이 변했으면 `05-progress/current.md`만 갱신한다. 완료 이력을 쌓지 않는다.
4. 상세 변경 내역의 원본은 Git이다. 별도 로그 파일을 만들지 않는다.

## 게임 스펙의 원본 위치

- 컨셉과 심사기준 대응: `docs/01-product/concept.md`
- 확정 게임 규칙과 조작: `docs/02-design/game-rules.md`
- 아키텍처와 결정론 규칙: `docs/03-tech/architecture.md`
- 캡처 파이프라인 스펙: `docs/03-tech/capture-pipeline.md`

여기(AGENTS.md)에 스펙을 복제하지 않는다.
